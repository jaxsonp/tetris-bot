[project]
name = "tetris"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
tetris_game = { workspace = true }
tetris_cli_player = { workspace = true }
